'''
3. What does the id() function return? Is the value returned by id() same for two variables holding the same value?

Ans - id() function return the address of the variable
Immutable variables can share the same address, while the immutable objects always gets their own memory
'''